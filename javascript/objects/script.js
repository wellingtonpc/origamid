var Person = {
    name: "John",
    age: 30
};

var Square = {
    side: 5,
    area: function() {
        return this.side * this.side;
    },
    perimeter: function() {
        return 4 * this.side;
    }
};   
console.log(Math.random()); // Generates a random number between 0 and 

// operador ternário
var idade;
var maiorIdade = (idade > 18) ? "Adulto": "Menor de idade";


const num2 = 5;
function sumTwoNumbers(num1) {
    return num1 + num2;
}

function division(num1) {
    return num1 / num2;
}